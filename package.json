{
  "name": "workshot",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node dist/index.js",
    "test": "vitest run"
  },
  "dependencies": {
    "sharp": "0.33.5"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3",
    "vitest": "^3.0.0"
  }
}
