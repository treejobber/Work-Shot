# WorkShot

Internal tool for before/after photo workflows.

## Quick Start

```bash
npm install
npm run build
npm start
```

## Structure

- `src/` — Application source
- `jobs/` — Input photos (local only, not tracked)
- `out/` — Generated outputs (local only, not tracked)
- `docs/` — Documentation
