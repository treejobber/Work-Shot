# Example Job Folder

This folder documents the expected structure for a job.

## Required Files

- `before.jpg` (or `before.jpeg` or `before.png`)
- `after.jpg` (or `after.jpeg` or `after.png`)

## Optional Files

- `meta.json` — Job metadata

### meta.json schema

```json
{
  "service": "tree trim",
  "notes": "Optional note about the work"
}
```

## No Images Included

This folder is for documentation only. Add your own before/after images to run the pipeline.
